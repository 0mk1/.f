# function kubectl { docker run --rm --user root -v $HOME/.kube:/root/.kube bitnami/kubectl:${KUBECTL_VERSION} $@ }
# function helm { docker run --rm -it --user root --entrypoint=/bin/sh -v $HOME:/root:ro gcr.io/kubernetes-helm/tiller:v2.12.3 $@; }
function index { python -c "import sys; print(sys.argv[1][$2 - 1])" $1 }

alias awsp="_awsSwitchProfile"
alias c="cd ~/code/"
alias dcmp="docker-compose "
alias ddev="docker-compose -f dev.yml"
alias g="git"
alias k="kubectl "
alias kctx="kubectx "
alias kens="kubens "
alias l="ls -la"
alias ta="tmux -2 attach -t "
alias tm="tmux -2"
alias v="nvim"
alias vi="nvim"
alias vim="nvim"
alias es-fix-95-storage="curl -XPUT -H "Content-Type: application/json" http://user:password@localhost:9200/*/_settings -d '{"index.blocks.read_only_allow_delete": null}'"
alias tcpdump="tcpdump -l -A -i eth1 port 5000"
alias calvertag="git tag -a $(date +%Y.%m.%d)"
alias tr="docker-compose run --rm -it --service-ports terraform"
alias passcp="cat ~/code/pass.md | pbcopy"
